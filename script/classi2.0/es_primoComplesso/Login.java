/*import java.util.ArrayList;
import java.util.Arrays;
import java.math.*;

public class Login {

    private String nome;
    private String password;
    private double budget=Math.random()*100;
    private ArrayList<String> nomiS= new ArrayList<String>();
    private ArrayList<String> passwordS= new ArrayList<String>();

    public Login(String nome,String password,String[] nomi, String[] passwords) {
            this.nome = nome;
            this.password=password;
            this.nomiS = new ArrayList<>(Arrays.asList(nomi));
            this.passwordS = new ArrayList<>(Arrays.asList(passwords));
            for(int i=0;i<numeroRegistrati;i++){
                if(nome.equals(nomiS(i))){
                    if(password.equals(passwordS(i))){
                        System.out.println("Login avvenuto con successo");

                    }
                }
            }
        }

    public NumeroRegistrati(int numeroRegistrati){
        this.numeroRegistrati=numero registrati;
    }
    

    public String getNome() {
        return nome;
    }

    public double getBudget() {
        return budget;
    }
}*/